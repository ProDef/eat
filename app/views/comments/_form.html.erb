<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :reviewer %><br />
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :thoughts %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit "post review" %>
  </p>
<% end %>